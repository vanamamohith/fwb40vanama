extends layout

block content
  h1= title
  - const usa  = ['Van_type','Model','year']
  - let results;
  
  script.
    function buy(x){
         alert('Purchase Item: '+x)

   } 
  div(class='container-fluid')
    div(class='row')
      each results, index in usa 
        div(class='col-sm-2' onclick='buy('+index+')') #{results}